// TODO: Implement tambah_page.dart
import 'package:flutter/material.dart';

class TambahPage extends StatelessWidget {
  const TambahPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tambah Pengeluaran')),
      body: Center(
        child: Text('Halaman Tambah Pengeluaran - Belum diimplementasikan'),
      ),
    );
  }
}