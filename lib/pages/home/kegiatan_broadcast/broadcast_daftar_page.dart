// TODO: Implement broadcast_daftar_page.dart
import 'package:flutter/material.dart';

class BroadcastDaftarPage extends StatelessWidget {
  const BroadcastDaftarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Broadcast - Daftar')),
      body: Center(
        child: Text('Halaman Broadcast - Daftar - Belum diimplementasikan'),
      ),
    );
  }
}